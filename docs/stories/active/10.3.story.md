---
id: "10.3"
title: "SYNAPSE — MIS Retrieval Layer: memory-retriever.js + memory-loader.js Migration"
status: Draft
priority: High
epic: "10 — SYNAPSE Integration: MIS Core + Injection Engine"
prd: "docs/prd/synapse-context-injection-engine.md"
prd_ref: "Epic 1, Story 1.3"
created: "2026-03-02"
updated: "2026-03-02"
owner: "@dev (Dex)"
validator: "@qa (Quinn)"
type: Architecture
complexity: Medium
blocked_by: "10.2"
---

# Story 10.3 — SYNAPSE: MIS Retrieval Layer: memory-retriever.js + memory-loader.js Migration

## Status

Draft

## Story

**As a** developer integrating the MIS retrieval engine into the repository,
**I want** `memory-retriever.js` and `memory-loader.js` migrated and operational with all 76 tests passing,
**so that** agents can query memories by tier, tag, and token budget using the 3-layer progressive disclosure pattern.

## Acceptance Criteria

1. `memory-retriever.js` migrated to `.aios-core/core/synapse/memory/memory-retriever.js` with internal `require('./memory-index')` updated to match new path
2. `memory-loader.js` migrated to `.aios-core/core/synapse/memory/memory-loader.js` with internal `require('./memory-retriever')` updated to match new path
3. All 76 unit tests migrated: 45 retriever tests to `tests/core/synapse/memory/memory-retriever.test.js` and 31 loader tests to `tests/core/synapse/memory/memory-loader.test.js`
4. 3-layer auto-selection verified by tests: `tokenBudget < 500 → L1` | `< 1500 → L2` | `≥ 1500 → L3`
5. Agent-scoped privacy verified by tests: a `dev` agent query cannot retrieve `qa` agent memories
6. All `AGENT_SECTOR_PREFERENCES` preserved in `memory-loader.js` and covered by tests
7. `npm test -- tests/core/synapse/memory/` passes with **zero failures** across both test files
8. Coverage on both files is **≥ 90.7%**
9. `npm run lint` passes on all new/modified files

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Architecture
**Secondary Type(s)**: Integration (brownfield migration — 2 files)
**Complexity**: Medium — 718 lines total (451 + 267), 76 tests, dependency chain

### Specialized Agent Assignment

**Primary Agents**:
- @dev (migration, dependency path update, test validation)
- @architect (validates 3-layer pattern implementation, agent-scoped privacy)

**Supporting Agents**:
- @qa (coverage verification ≥ 90.7% across both files)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run `wsl bash -c 'cd /mnt/c/Users/markm/OneDrive/Documentos/GitHub/aios-core && ~/.local/bin/coderabbit --prompt-only -t uncommitted'` before marking story complete
- [ ] Pre-PR (@github-devops): Run `wsl bash -c 'cd /mnt/c/Users/markm/OneDrive/Documentos/GitHub/aios-core && ~/.local/bin/coderabbit --prompt-only --base main'` before creating pull request

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL only

**Predicted Behavior**:
- CRITICAL issues: auto_fix (up to 2 iterations)
- HIGH issues: document_only (noted in Dev Notes)
- MEDIUM/LOW: ignore

### CodeRabbit Focus Areas

**Primary Focus**:
- Dependency chain integrity: `memory-loader` → `memory-retriever` → `memory-index` — all internal requires must resolve
- No yaml import (neither file uses yaml — no migration needed)
- Module exports preserved exactly (callers in 10.4+ depend on these shapes)

**Secondary Focus**:
- Agent-scoped privacy: verify no cross-agent memory leakage in retriever logic
- `AGENT_SECTOR_PREFERENCES` constant intact in `memory-loader.js`
- Layer auto-selection thresholds (500/1500) preserved in retriever

## Tasks / Subtasks

- [ ] Task 1 — Verify prerequisites (AC: prerequisite)
  - [ ] Confirm Story 10.2 is Done: `.aios-core/core/synapse/memory/memory-index.js` exists
  - [ ] Confirm `{ MemoryIndexManager }` is importable from `memory-index.js`

- [ ] Task 2 — Migrate `memory-retriever.js` (AC: 1)
  - [ ] Copy source: `C:\Users\markm\OneDrive\Pesquisas e decisoes Upgrade Neo\memory\memory-retriever.js`
  - [ ] Destination: `.aios-core/core/synapse/memory/memory-retriever.js`
  - [ ] Update `require('./memory-index')` — path is already correct (same directory) ✓
  - [ ] Update module header comment: `@module core/synapse/memory/memory-retriever`
  - [ ] Verify exports: `module.exports = { MemoryRetriever, TOKEN_ESTIMATES }`

- [ ] Task 3 — Migrate `memory-loader.js` (AC: 2)
  - [ ] Copy source: `C:\Users\markm\OneDrive\Pesquisas e decisoes Upgrade Neo\memory\memory-loader.js`
  - [ ] Destination: `.aios-core/core/synapse/memory/memory-loader.js`
  - [ ] Update `require('./memory-retriever')` — path is already correct (same directory) ✓
  - [ ] Update module header comment: `@module core/synapse/memory/memory-loader`
  - [ ] Verify exports: `module.exports = { MemoryLoader, createMemoryLoader, AGENT_SECTOR_PREFERENCES }`

- [ ] Task 4 — Migrate retriever test file (AC: 3)
  - [ ] Copy source: `C:\Users\markm\OneDrive\Pesquisas e decisoes Upgrade Neo\memory\__tests__\memory-retriever.test.js`
  - [ ] Destination: `tests/core/synapse/memory/memory-retriever.test.js`
  - [ ] Update require path to `../../../../.aios-core/core/synapse/memory/memory-retriever`
  - [ ] Verify 45 test cases present (grep `it(` — expect 45 matches)

- [ ] Task 5 — Migrate loader test file (AC: 3)
  - [ ] Copy source: `C:\Users\markm\OneDrive\Pesquisas e decisoes Upgrade Neo\memory\__tests__\memory-loader.test.js`
  - [ ] Destination: `tests/core/synapse/memory/memory-loader.test.js`
  - [ ] Update require path to `../../../../.aios-core/core/synapse/memory/memory-loader`
  - [ ] Verify 31 test cases present (grep `it(` — expect 31 matches)

- [ ] Task 6 — Run and verify all tests (AC: 4, 5, 6, 7, 8)
  - [ ] Run: `npm test -- tests/core/synapse/memory/memory-retriever.test.js` — verify 45 pass
  - [ ] Run: `npm test -- tests/core/synapse/memory/memory-loader.test.js` — verify 31 pass
  - [ ] Confirm L1/L2/L3 selection tests pass (tokenBudget thresholds)
  - [ ] Confirm agent-scoped privacy test passes (dev cannot access qa memories)
  - [ ] Run: `npm run test:coverage` — verify ≥ 90.7% on both files

- [ ] Task 7 — Lint and quality check (AC: 9)
  - [ ] Run `npm run lint` on both migrated files
  - [ ] Run Pre-Commit CodeRabbit gate (see Quality Gate Tasks above)

## Dev Notes

### Prerequisites

**Story 10.2 must be Done** before starting. `memory-retriever.js` imports `MemoryIndexManager` from `memory-index.js`.

```
Check: ls .aios-core/core/synapse/memory/memory-index.js  → must exist
```

### Source Files

```
SOURCE (OneDrive):
  C:\Users\markm\OneDrive\Pesquisas e decisoes Upgrade Neo\memory\memory-retriever.js     (451 lines)
  C:\Users\markm\OneDrive\Pesquisas e decisoes Upgrade Neo\memory\memory-loader.js        (267 lines)
  C:\Users\markm\OneDrive\Pesquisas e decisoes Upgrade Neo\memory\__tests__\memory-retriever.test.js  (45 tests)
  C:\Users\markm\OneDrive\Pesquisas e decisoes Upgrade Neo\memory\__tests__\memory-loader.test.js     (31 tests)

DESTINATION (repo):
  .aios-core/core/synapse/memory/memory-retriever.js
  .aios-core/core/synapse/memory/memory-loader.js
  tests/core/synapse/memory/memory-retriever.test.js
  tests/core/synapse/memory/memory-loader.test.js
```

### No yaml Migration Needed

**Neither file uses `require('yaml')`** — confirmed by inspection. No API changes needed beyond path updates.

### Dependency Chain (internal requires — all relative, same directory)

```
memory-loader.js
  └── requires memory-retriever.js  (require('./memory-retriever'))
        └── requires memory-index.js  (require('./memory-index'))
              └── requires feature-gate.js  (from license/)
```

Since all files land in `.aios-core/core/synapse/memory/`, the relative requires `./memory-retriever` and `./memory-index` remain correct after migration. **No path changes needed** for internal requires.

Only the test files need their require paths updated (tests are in a different directory tree).

### Module Exports

```javascript
// memory-retriever.js
module.exports = { MemoryRetriever, TOKEN_ESTIMATES };

// memory-loader.js
module.exports = { MemoryLoader, createMemoryLoader, AGENT_SECTOR_PREFERENCES };
```

These exact shapes are consumed by `synapse-memory-provider.js` in Story 10.4. **Do not alter exports.**

### 3-Layer Progressive Disclosure (memory-retriever.js)

The retriever implements auto-selection based on tokenBudget:

| Budget | Layer | Token Estimate | Description |
|--------|-------|----------------|-------------|
| < 500 | L1 | ~50 tokens | Metadata only |
| < 1500 | L2 | ~200 tokens | Relevant chunks |
| ≥ 1500 | L3 | ~1000+ tokens | Full content |

These thresholds are defined as constants in `TOKEN_ESTIMATES` and tested in the test file. Preserve exactly.
[Source: PRD-SYNAPSE-001 v0.2, FR2]

### Agent-Scoped Privacy

`memory-loader.js` enforces that each agent accesses only its own memories plus shared memories. The `dev` agent must NOT be able to retrieve memories scoped to `qa`. This is enforced via the `agentId` filter in the retriever.

Test for this: look for a test case like `"dev agent cannot access qa memories"` or similar in the test file.
[Source: PRD-SYNAPSE-001 v0.2, FR2]

### AGENT_SECTOR_PREFERENCES

`memory-loader.js` exports `AGENT_SECTOR_PREFERENCES` — a mapping of agent IDs to their preferred cognitive sectors:

```javascript
const AGENT_SECTOR_PREFERENCES = {
  dev: ['procedural', 'semantic'],
  // ... other agents
};
```

This constant must be preserved verbatim and is tested. Do not alter values.
[Source: OneDrive memory-loader.js inspection 2026-03-02]

### Test Import Path

From `tests/core/synapse/memory/` to `.aios-core/core/synapse/memory/`:

```javascript
// In retriever test file — update from:
const { MemoryRetriever } = require('../memory-retriever');
// To:
const { MemoryRetriever } = require('../../../../.aios-core/core/synapse/memory/memory-retriever');

// In loader test file — update from:
const { MemoryLoader, createMemoryLoader } = require('../memory-loader');
// To:
const { MemoryLoader, createMemoryLoader } = require('../../../../.aios-core/core/synapse/memory/memory-loader');
```

Verify the actual require path in the source test files before updating.

### Relevant Source Tree

```
.aios-core/core/synapse/memory/
  ├── memory-index.js       ← EXISTS (Story 10.2)
  ├── memory-retriever.js   ← NEW (this story)
  └── memory-loader.js      ← NEW (this story)
tests/core/synapse/memory/
  ├── memory-index.test.js   ← EXISTS (Story 10.2)
  ├── memory-retriever.test.js  ← NEW (this story)
  └── memory-loader.test.js    ← NEW (this story)
```

### Testing

- **Test framework:** Jest
- **Run retriever tests:** `npm test -- tests/core/synapse/memory/memory-retriever.test.js`
- **Run loader tests:** `npm test -- tests/core/synapse/memory/memory-loader.test.js`
- **Run all synapse memory tests:** `npm test -- tests/core/synapse/memory/`
- **Expected:** 45 + 31 = 76 tests pass, coverage ≥ 90.7%

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-02 | 0.1 | Initial draft | River (SM) |

## Dev Agent Record

### Agent Model Used

_To be filled by @dev_

### Debug Log References

_To be filled by @dev_

### Completion Notes List

_To be filled by @dev_

### File List

_To be filled by @dev_

## QA Results

_To be filled by @qa_
