---
id: "10.2"
title: "SYNAPSE — MIS Storage Layer: memory-index.js Migration"
status: Draft
priority: High
epic: "10 — SYNAPSE Integration: MIS Core + Injection Engine"
prd: "docs/prd/synapse-context-injection-engine.md"
prd_ref: "Epic 1, Story 1.2"
created: "2026-03-02"
updated: "2026-03-02"
owner: "@dev (Dex)"
validator: "@qa (Quinn)"
type: Architecture
complexity: Medium
blocked_by: "10.1"
---

# Story 10.2 — SYNAPSE: MIS Storage Layer: memory-index.js Migration

## Status

Ready for Review

## Story

**As a** developer integrating the Memory Intelligence System into the repository,
**I want** `memory-index.js` migrated and operational with all 33 tests passing,
**so that** session digests can be indexed and searched as the storage foundation for the MIS pipeline.

## Acceptance Criteria

1. `memory-index.js` migrated to `.aios-core/core/synapse/memory/memory-index.js` with `require('yaml')` replaced by `require('js-yaml')` and all `yaml.parse()` calls replaced by `yaml.load()`
2. All 33 unit tests migrated to `tests/core/synapse/memory/memory-index.test.js` with test imports updated to match new file path
3. Index build test passes: 100 digests indexed in < 2 seconds (performance test at line ~533 of source test file)
4. Search performance test passes: 500 memories searched in < 50ms (performance test at line ~567 of source test file)
5. `npm test -- tests/core/synapse/memory/memory-index.test.js` passes with **zero failures**
6. Coverage on `memory-index.js` is **≥ 90.7%** (run `npm run test:coverage` and verify)
7. `npm run lint` passes on all new/modified files

## 🤖 CodeRabbit Integration

### Story Type Analysis

**Primary Type**: Architecture
**Secondary Type(s)**: Integration (brownfield migration)
**Complexity**: Medium — 478-line file, 33 tests, performance benchmarks required

### Specialized Agent Assignment

**Primary Agents**:
- @dev (migration, yaml API update, test validation)
- @architect (architectural pattern validation — CommonJS, module boundaries)

**Supporting Agents**:
- @qa (coverage verification ≥ 90.7%)

### Quality Gate Tasks

- [ ] Pre-Commit (@dev): Run `wsl bash -c 'cd /mnt/c/Users/markm/OneDrive/Documentos/GitHub/aios-core && ~/.local/bin/coderabbit --prompt-only -t uncommitted'` before marking story complete
- [ ] Pre-PR (@github-devops): Run `wsl bash -c 'cd /mnt/c/Users/markm/OneDrive/Documentos/GitHub/aios-core && ~/.local/bin/coderabbit --prompt-only --base main'` before creating pull request

### Self-Healing Configuration

**Expected Self-Healing**:
- Primary Agent: @dev (light mode)
- Max Iterations: 2
- Timeout: 15 minutes
- Severity Filter: CRITICAL only

**Predicted Behavior**:
- CRITICAL issues: auto_fix (up to 2 iterations)
- HIGH issues: document_only (noted in Dev Notes)
- MEDIUM/LOW: ignore

### CodeRabbit Focus Areas

**Primary Focus**:
- `yaml` → `js-yaml` API migration correctness (`yaml.parse` → `yaml.load`, `yaml.stringify` → `yaml.dump`)
- No new external dependencies introduced (NFR6)
- Module exports preserved: `{ MemoryIndexManager }` — callers in 10.3+ depend on this shape

**Secondary Focus**:
- Performance tests must remain as automated assertions (not commented out)
- CommonJS pattern consistency with rest of `.aios-core/core/`
- Paths reference `.aios/session-digests/` correctly (runtime storage, not source)

## Tasks / Subtasks

- [x] Task 1 — Verify prerequisite (AC: prerequisite)
  - [x] Confirm Story 10.1 is Done: `.aios-core/core/synapse/license/feature-gate.js` exists
  - [x] Confirm `.aios-core/core/synapse/memory/` directory exists (created in 10.1)

- [x] Task 2 — Migrate `memory-index.js` (AC: 1)
  - [x] Copy source: `C:\Users\markm\OneDrive\Pesquisas e decisoes Upgrade Neo\memory\memory-index.js`
  - [x] Destination: `.aios-core/core/synapse/memory/memory-index.js`
  - [x] Replace `require('yaml')` → `require('js-yaml')` (line ~13 of source)
  - [x] Replace all `yaml.parse(` calls → `yaml.load(`
  - [x] Replace all `yaml.stringify(` calls → `yaml.dump(` (verify if any exist)
  - [x] Verify module header comment references new module path: `@module core/synapse/memory/memory-index`
  - [x] Verify `module.exports = { MemoryIndexManager }` is preserved at end of file

- [x] Task 3 — Migrate test file (AC: 2)
  - [x] Create `tests/core/synapse/memory/` directory if not exists
  - [x] Copy source: `C:\Users\markm\OneDrive\Pesquisas e decisoes Upgrade Neo\memory\__tests__\memory-index.test.js`
  - [x] Destination: `tests/core/synapse/memory/memory-index.test.js`
  - [x] Update `require('../memory-index')` (or similar) → `require('../../../../.aios-core/core/synapse/memory/memory-index')`
  - [x] Verify all 33 test cases are present (grep for `it(` — should find 33 matches)

- [x] Task 4 — Run and verify tests (AC: 3, 4, 5, 6)
  - [x] Run: `npm test -- tests/core/synapse/memory/memory-index.test.js`
  - [x] Verify 0 failures, 33 tests pass
  - [x] Confirm performance test passes: "should rebuild 100 digests in < 2 seconds"
  - [x] Confirm performance test passes: "should search 500 memories in < 50ms"
  - [x] Run: `npm run test:coverage` — verify `memory-index.js` coverage ≥ 90.7%

- [x] Task 5 — Lint and quality check (AC: 7)
  - [x] Run `npm run lint` on `.aios-core/core/synapse/memory/memory-index.js`
  - [ ] Run Pre-Commit CodeRabbit gate (see Quality Gate Tasks above)

## Dev Notes

### Prerequisite

**Story 10.1 must be Done** before starting this story. The feature-gate.js stub must exist at `.aios-core/core/synapse/license/feature-gate.js`.

Check: `ls .aios-core/core/synapse/license/feature-gate.js` should return the file.

### Source Files

```
SOURCE (OneDrive):
  C:\Users\markm\OneDrive\Pesquisas e decisoes Upgrade Neo\memory\memory-index.js
  C:\Users\markm\OneDrive\Pesquisas e decisoes Upgrade Neo\memory\__tests__\memory-index.test.js

DESTINATION (repo):
  .aios-core/core/synapse/memory/memory-index.js
  tests/core/synapse/memory/memory-index.test.js
```

### yaml → js-yaml Migration (REQUIRED)

The source file uses `const yaml = require('yaml')` (line ~13). The repo uses `js-yaml`.

**Step 1 — Change require:**
```javascript
// FROM:
const yaml = require('yaml');
// TO:
const yaml = require('js-yaml');
```

**Step 2 — Update API calls:**
```javascript
// yaml.parse() → yaml.load()
const data = yaml.parse(fileContent);  // FROM
const data = yaml.load(fileContent);   // TO

// yaml.stringify() → yaml.dump()
const str = yaml.stringify(data);  // FROM (verify if used)
const str = yaml.dump(data);       // TO
```

Run `grep -n "yaml\." .aios-core/core/synapse/memory/memory-index.js` after copy to find all usages.

### Module Structure

- **Class:** `MemoryIndexManager`
- **Constructor:** `new MemoryIndexManager(projectDir)` — `projectDir` defaults to `process.cwd()`
- **Key directories used at runtime:**
  - `{projectDir}/.aios/session-digests/` — reads digest files
  - `{projectDir}/.aios/session-digests/index/` — writes index files
- **Exports:** `module.exports = { MemoryIndexManager }`
- **Size:** 478 lines
- [Source: OneDrive memory-index.js inspection 2026-03-02]

### Performance Benchmarks (must pass as automated tests)

These are hard performance requirements, already built into the test file:

| Test | Requirement | Location in test file |
|------|-------------|----------------------|
| Index build | 100 digests < 2s | `it('should rebuild 100 digests in < 2 seconds')` ~line 533 |
| Search | 500 memories < 50ms | `it('should search 500 memories in < 50ms')` ~line 567 |

These tests were confirmed achieving ~70ms (28x faster) and ~8-10ms (5x faster) on the original machine. They should pass easily.
[Source: PRD-SYNAPSE-001 v0.2, NFR3, NFR4]

### 33 Test Cases Summary

Tests cover:
- `buildIndex()` — builds from digest files, handles multiple digests, skips examples, handles missing frontmatter
- `loadIndexes()` — loads from disk, rebuilds if loading fails
- `search()` — by agent, date, tags, tier, combined filters, limit, sort by attention score, performance
- `updateIndex()` — incremental update, rebuild from scratch
- `_calculateAttentionScore()` — all factors, recency decay, trigger boosts, duration boosts
- `_classifyTier()` — HOT/WARM/COLD thresholds
- `_detectSector()` — procedural/semantic/reflective/episodic
- `_extractTags()` — from patterns, corrections, axioms, normalization
- Performance benchmarks (2 tests)

### Test Import Path

After copying, update the test file's require path. The source likely has something like:
```javascript
const { MemoryIndexManager } = require('../memory-index');
```
Change to:
```javascript
const { MemoryIndexManager } = require('../../../../.aios-core/core/synapse/memory/memory-index');
```
Or use a relative path from `tests/core/synapse/memory/` to `.aios-core/core/synapse/memory/`.

Verify the exact relative path: from `tests/core/synapse/memory/` to `.aios-core/core/synapse/memory/` is `../../../../.aios-core/core/synapse/memory/`.

### Relevant Source Tree

```
.aios-core/core/synapse/memory/memory-index.js   ← NEW (migrated this story)
tests/core/synapse/memory/memory-index.test.js   ← NEW (migrated this story)
.aios-core/core/synapse/license/feature-gate.js  ← EXISTS (from Story 10.1)
.aios/session-digests/                           ← runtime dir (may not exist yet; tests use mocks)
```

### Testing

- **Test framework:** Jest
- **Test file:** `tests/core/synapse/memory/memory-index.test.js`
- **Run single file:** `npm test -- tests/core/synapse/memory/memory-index.test.js`
- **Run with coverage:** `npm run test:coverage -- tests/core/synapse/memory/memory-index.test.js`
- **Expected:** 33 tests pass, coverage ≥ 90.7%
- Tests use mocked filesystem (check source test for `jest.mock('fs')` or similar)

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-02 | 0.1 | Initial draft | River (SM) |
| 2026-03-02 | 1.0 | Migration complete — 33/33 tests pass, coverage 97.63% lines | Dex (dev) |

## Dev Agent Record

### Agent Model Used

claude-sonnet-4-6

### Debug Log References

None — clean migration, all tests passed on first run.

### Completion Notes List

1. **Files were pre-migrated:** `memory-index.js` and `memory-index.test.js` were already copied from OneDrive and present as untracked files in the working tree. Migration was complete including `yaml → js-yaml` swap and `yaml.load()` API usage. An additional `js-yaml` Date object normalization was already applied for timestamp handling.

2. **Lint auto-fix applied:** 39 `comma-dangle` warnings (trailing commas) auto-fixed via `eslint --fix` on both source and test file. Zero errors before and after.

3. **Coverage exceeds target:** Lines 97.63%, Statements 95.65% — both well above the 90.7% AC threshold. Branch coverage 84.09% is acceptable for a brownfield migration with no new logic.

4. **CodeRabbit:** Skipped — no WSL environment available. Code is a direct migration with formatting-only changes.

5. **Performance benchmarks:** 100 digests in 96ms (20x faster than 2s limit); search 500 memories in <1ms (50x faster than 50ms limit).

### File List

- `.aios-core/core/synapse/memory/memory-index.js` — MIGRATED (yaml→js-yaml, yaml.parse→yaml.load, comma-dangle lint fix)
- `tests/core/synapse/memory/memory-index.test.js` — MIGRATED (import path updated, comma-dangle lint fix)

## QA Results

### Review Date: 2026-03-02

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementation is clean, correct, and precisely scoped. The migration faithfully applies the `yaml → js-yaml` API change with one critical correctness addition: `js-yaml` parses ISO-8601 timestamps as native `Date` objects (unlike the `yaml` package which returns strings), and the migrated code handles both cases explicitly in `_indexDigestFile()` (line 114–117) and `_calculateAttentionScore()` (line 210–211). This normalization prevents silent runtime failures that would have occurred on first use. The addition was pre-applied before this story was reviewed — it is correct and necessary.

Module structure, CommonJS patterns, JSDoc documentation, and export shape (`{ MemoryIndexManager }`) all conform to `.aios-core/core/` conventions. File header `@module core/synapse/memory/memory-index` is correct.

Test quality is high: 33 tests across 8 describe blocks using real filesystem with proper `beforeEach`/`afterEach` cleanup. No jest.mock needed — the real fs calls with temp dirs is the right strategy for a file-system-dependent module. Performance tests carry explicit timeouts (10s, 15s) and measure only the operation under test, not setup time.

### AC Verification Matrix

| AC | Requirement | Status | Evidence |
|----|-------------|--------|----------|
| AC-1 | `js-yaml` used, `yaml.load()` API | PASS | Line 12: `require('js-yaml')`, line 108: `yaml.load()`. No `yaml.parse()`, no `yaml.stringify()` found. |
| AC-2 | 33 tests at `tests/core/synapse/memory/memory-index.test.js` | PASS | 33 `it()` blocks confirmed. Import path `../../../../.aios-core/core/synapse/memory/memory-index` is correct. |
| AC-3 | 100 digests indexed in < 2s | PASS | 97ms measured (20× faster than limit) |
| AC-4 | 500 memories searched in < 50ms | PASS | `duration` measured inside test is < 1ms; search operates on in-memory index after build |
| AC-5 | `npm test` — zero failures | PASS | 33/33 passing, independently verified by QA |
| AC-6 | Coverage ≥ 90.7% | PASS | Lines: 97.63%, Statements: 95.65%. Branch: 84.09% (uncovered: error catch blocks at lines 88–89, 164, 462 — acceptable for brownfield migration) |
| AC-7 | `npm run lint` passes | PASS | 0 errors, 0 warnings (39 `comma-dangle` warnings auto-fixed by `eslint --fix` during dev) |

### Refactoring Performed

None. The implementation is clean and correct as-delivered. No refactoring was warranted.

### Compliance Check

- Coding Standards: PASS — CommonJS pattern, kebab-case filename, single-quote strings, JSDoc on all public and private methods
- Project Structure: PASS — Source at `.aios-core/core/synapse/memory/`, tests at `tests/core/synapse/memory/` — both match source-tree.md conventions
- Testing Strategy: PASS — Jest unit tests, real filesystem fixtures, cleanup on teardown, performance benchmarks as automated assertions
- All ACs Met: PASS — 7/7 acceptance criteria verified

### Improvements Checklist

- [x] All acceptance criteria verified against actual code and test output
- [x] 33 tests independently executed and passing (QA run)
- [x] yaml API migration confirmed: exactly one `yaml.load()` call, zero legacy `yaml.parse()` calls
- [x] `js-yaml` Date object normalization verified as correct and necessary
- [x] Export shape `{ MemoryIndexManager }` confirmed — downstream stories 10.3+ are unblocked
- [x] Lint independently verified: 0 errors, 0 warnings
- [ ] CodeRabbit pre-commit scan — not run (no WSL environment). Recommended before PR creation (@devops gate)

### Security Review

No security concerns. The module reads YAML files from a developer-controlled `projectDir`, writes JSON index files to the same tree, has no HTTP calls, no external service dependencies, no user-supplied input executed, no secrets handling, and no eval. Path construction uses `path.join()` with known subdirectory names — no traversal risk for standard usage.

### Performance Considerations

Performance characteristics exceed requirements by large margins (20× on build, 50×+ on search). The in-memory index design with Set-based intersection is an appropriate O(n)/O(1) strategy for the stated scale (≤500 memories). No changes needed.

### Files Modified During Review

None. Review was read-only. No source or test modifications were made.

### Gate Status

Gate: PASS → docs/qa/gates/10.2-synapse-memory-index-migration.yml
Quality Score: 98/100 (2 points for missing CodeRabbit scan — environment constraint, not a code quality issue)

### Recommended Status

PASS — Ready for Done. All 7 acceptance criteria verified, 33/33 tests passing, coverage exceeds threshold, lint clean. Downstream stories 10.3+ are unblocked. Story owner may proceed to Done.
